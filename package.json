{
  "name": "blingblaw",
  "version": "4.2.2",
  "author": "Danzilla",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danzilla/blingBlaw.git"
  },
  "scripts": {
    "docker-clean": "docker-compose rm -f && docker-compose build --no-cache",
    "start-prod": "docker-compose rm -f && docker-compose build --no-cache && docker-compose up --force-recreate",
    "start-dev": "docker-compose up -d pgadmin && docker-compose up postgres_db server client"
  }
}
