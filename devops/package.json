{
  "name": "blingblaw_v3",
  "version": "3.0.0",
  "description": "Client and Server - Development and Operations",
  "scripts": {
    "start-server": "cd ../server && npm start",
    "start-client": "cd ../client && npm start",
    "start-docker-dev": "docker-compose up --build",
    "install-server": "cd ../server && npm install",
    "install-client": "cd ../client && npm install",
    "postinstall": "npm run install-server && npm run install-client",
    "start": "concurrently --kill-others \"npm run start-server\" \"npm run start-client\" \"npm run start-docker-dev\""
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/danzilla/blingBlaw.git"
  },
  "author": "Danzilla",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^4.1.0"
  }
}
