<h3 class="text-center display-5">summary <small> <%- pageInfo.title %></small></h3>
<hr>

<!-- pie chart canvas element -->
<canvas id="myChart" width="400" height="400"></canvas>
<script>

  new Chart(document.getElementById("myChart"), {
      type: 'pie',
      data: {
        labels: <%- JSON.stringify(chartInfo.dataLabel) %>,
        datasets: [{
          label: "Category and subcategory",
          backgroundColor: <%- JSON.stringify(chartInfo.dataColor) %>,
          data: [<%= chartInfo.dataValue %>]
        }]
      },
      options: {
        title: {
          display: true,
          text: '<%= chartInfo.chartName %>'
        }
      }
  });

</script>
